# Deferred Production Register

This register tracks items intentionally deferred until a later production-readiness phase.

| Item | Category | Reason Deferred | Risk If Forgotten | Latest Safe Phase | Recommendation |
| --- | --- | --- | --- | --- | --- |
| Auth password hashing implementation | Security | Phase 23.2 defines credential storage fields only. | Password credentials could be modeled without a governed hashing and verification implementation. | Before Auth Service login runtime | Implement approved password hashing, parameter versioning, migration tests, and secret-handling QA. |
| Auth MFA implementation | Security | Phase 23.2 defines TOTP/WebAuthn credential boundaries only. | High-risk identities may lack required second-factor controls. | Before Auth Service production login | Implement MFA enrollment, challenge, recovery, disable, and audit workflows. |
| Auth OAuth/OIDC runtime | Security | Phase 23.2 models OAuth clients and tokens only. | Services cannot rely on the Auth Service as an identity provider. | Before service auth migration | Implement OAuth2/OIDC endpoints, discovery metadata, grant validation, consent rules, and conformance QA. |
| Auth JWKS signing key rotation | Security | Signing keys are modeled but no runtime rotation exists. | JWT validation could depend on unmanaged keys. | Before JWT issuance | Implement key generation/import, JWKS publication, rotation, retirement, and emergency revocation. |
| Current auth migration | Security | Existing platform auth remains unchanged. | Users, roles, and sessions may diverge across auth systems. | Before Auth Service cutover | Map current identities, roles, credentials, and permissions with rollback evidence. |
| Devtools/CI image hardening | Infrastructure | Phase 24.0 adds a local devtools service, but CI image publishing and cache strategy are not hardened. | Developers and CI may drift in tool versions or dependency caching behavior. | Before shared CI enforcement | Pin image versions, publish a CI image, add cache policy, and validate devtools in CI. |
| Migration runner selection | Persistence | Phase 24.0 inventories migrations but does not select the canonical runner. | Local, staging, and production migration application may diverge. | Before applying service schemas | Select one runner, define versioning, dry-run, rollback, and evidence procedures. |
| Local migration application | Persistence | Phase 24.0 does not apply migrations. | Repository implementations may be built against unapplied or drifting schemas. | Before database-backed repository activation | Apply local migrations through the selected runner and capture repeatable evidence. |
| Staging migration procedure | Persistence | Production and staging migrations are explicitly prohibited in Phase 24.0. | Staging may not rehearse ordering, rollback, or drift detection before production. | Before staging service activation | Define staging migration approval, backup, dry-run, rollback, and verification steps. |
| Auth Service compose registration | Integration | Auth Service remains optional/not registered in local compose. | Local integration may miss Auth Service health, diagnostics, and shadow import checks. | Before Auth Service runtime integration | Add Auth Service to compose with login/token/runtime traffic disabled. |
| Database-backed Game Engine repositories | Persistence | Game Engine schemas exist, but runtime repository wiring is not complete. | Evaluation records/checkpoints may remain in-memory and non-durable. | Before production evaluation activation | Implement database-backed repositories with transaction, retry, and append-only verification. |
| Database-backed Auth repositories | Persistence | Auth Service contracts and shadow read source exist, but production identity persistence repositories are inactive. | Auth Service cannot own durable identities, credentials, sessions, tokens, or audit evidence. | Before Auth Service cutover | Implement database-backed repositories behind disabled login/token runtime and migration approval gates. |
| Auth session migration | Security | Session migration is not designed in Phase 23.2. | Existing users could be logged out unexpectedly or stale sessions could remain valid. | Before Auth Service cutover | Design session coexistence, migration, revocation, and rollback controls. |
| PAM federation | Integration | PAM credential boundaries exist without provider implementation. | PAM users cannot authenticate through approved external sources. | Before PAM launch | Select PAM provider contracts and implement replay-safe federation mapping. |
| Service-to-service token validation | Security | Service trust is modeled but not enforced. | Services may continue using legacy trust without centralized validation. | Before service auth migration | Implement client credentials, optional mTLS binding, JWT validation, introspection, and service policy checks. |
| Auth break-glass workflow | Operations | Break-glass behavior remains in current platform auth. | Emergency access may not be governed after Auth Service migration. | Before Auth Service cutover | Rebuild break-glass accounts, approvals, expiry, audit, and recovery in Auth Service. |
| Auth password reset flow | Security | Phase 23.3 models reset-required and temporary-password flags only. | Users may lack governed recovery and admin reset flows in Auth Service. | Before Auth Service production login | Implement reset initiation, proofing, expiry, audit, and forced-reset workflows. |
| Auth TOTP enrollment and verification | Security | Phase 23.3 models TOTP only as an MFA method. | Privileged accounts may lack a functioning second factor. | Before Auth Service production login | Implement TOTP enrollment, challenge verification, disable rules, and recovery audit. |
| Auth WebAuthn/passkey implementation | Security | Phase 23.3 models WebAuthn/passkey verification contracts only. | Passwordless and phishing-resistant authentication may remain unavailable. | Before passwordless launch | Implement passkey registration, assertion verification, attestation policy, and device revocation. |
| Auth compromised password screening | Security | Phase 23.3 includes a screening placeholder only. | Known compromised passwords may be accepted after login migration. | Before password credential activation | Integrate an approved breached-password screening mechanism with privacy-preserving checks. |
| Auth recovery codes | Security | Phase 23.3 models recovery codes as placeholders only. | MFA recovery could become manual, inconsistent, or unaudited. | Before MFA enforcement | Implement one-time recovery codes with hashing, display-once behavior, rotation, and audit. |
| Auth step-up authentication | Security | Phase 23.3 models step-up requirements without runtime enforcement. | Privileged operations may proceed without fresh authentication proof. | Before privileged Auth Service operations | Implement policy-driven step-up challenges and local enforcement contracts. |
| Auth login endpoint migration | Migration | Phase 23.3 intentionally adds no login endpoint and does not wire Next.js. | Platform could split authentication behavior if migration is incomplete. | Before Auth Service cutover | Implement coexistence, routing, rollback, session migration, and QA evidence before switching login. |
| Auth signing key generation | Security | Phase 23.4 models signing key metadata only. | Token signing could be enabled without governed key lifecycle. | Before JWT issuance | Implement generation/import, activation, rotation, retirement, emergency revocation, and key custody evidence. |
| Auth JWKS publication | Security | Phase 23.4 models JWKS but does not publish runtime metadata. | Services cannot safely validate JWTs from Auth Service. | Before service token validation | Publish JWKS with cache headers, rotation overlap, and validation QA. |
| Auth token signing | Security | Phase 23.4 keeps token signing disabled. | Tokens may lack integrity guarantees if runtime is enabled prematurely. | Before token issuance | Implement approved signing algorithms, key selection, claims validation, and conformance tests. |
| Auth token introspection | Security | Opaque tokens require introspection, but runtime introspection is deferred. | Opaque tokens cannot be safely validated or revoked by services. | Before opaque token issuance | Implement introspection endpoint, client authorization, active-state checks, and audit. |
| Auth refresh token rotation runtime | Security | Phase 23.4 models rotation policy only. | Refresh token replay or reuse may not be detected. | Before refresh token issuance | Implement rotation storage, reuse detection, family revocation, and replay QA. |
| Auth OAuth authorization endpoint | Integration | Phase 23.4 models authorization requests but exposes no runtime endpoint. | OAuth clients cannot initiate approved flows. | Before OAuth client onboarding | Implement authorization endpoint, consent, PKCE, state validation, and redirect safety. |
| Auth OAuth token endpoint | Integration | Phase 23.4 models token requests but exposes no runtime endpoint. | OAuth grants cannot exchange for tokens. | Before OAuth client onboarding | Implement token endpoint with grant validation, client auth, signing, revocation, and audit. |
| Auth OIDC discovery endpoint | Integration | OIDC metadata is modeled but not published. | Clients and services cannot auto-discover issuer metadata. | Before OIDC client onboarding | Publish `.well-known/openid-configuration` with versioned metadata and QA. |
| Auth client credentials runtime | Security | Service client credentials are modeled but not accepted at runtime. | Extracted services cannot use centralized service trust. | Before service-to-service auth cutover | Implement client credentials grant, optional mTLS binding, scope enforcement, and audit. |
| Auth service-to-service enforcement | Security | Phase 23.4 models service auth but services do not enforce Auth tokens. | Services may continue legacy trust assumptions. | Before service auth migration | Add token validation, local policy enforcement, audience checks, and rollback controls to services. |
| Next.js Auth Service migration | Migration | Phase 23.4 does not wire Next.js to Auth Service. | Frontend auth may diverge from native Auth Service if migration is delayed. | Before Auth Service cutover | Implement coexistence, session bridge, rollback, login routing, and QA evidence. |
| Auth runtime login | Migration | Phase 23.5 defines migration only and keeps Auth Service login disabled. | Login traffic could remain on legacy auth indefinitely without a governed cutover. | Before Auth Service cutover | Implement login runtime behind approval-gated feature flags and coexistence QA. |
| Auth runtime token issuance | Security | Phase 23.5 keeps token issuance disabled during coexistence planning. | Services cannot validate Auth Service tokens until issuance is implemented safely. | Before service auth migration | Implement signing, refresh, revocation, introspection, and token conformance QA. |
| Production OAuth activation | Integration | OAuth/OIDC migration is planned but runtime endpoints remain disabled. | External clients and service auth cannot use Auth Service provider mode. | Before OAuth client onboarding | Activate authorization, token, discovery, JWKS, and consent endpoints after approval. |
| Legacy auth retirement | Migration | Legacy auth remains authoritative during coexistence planning. | Dual auth may persist longer than intended and increase operational complexity. | After approved Auth Service cutover | Retire legacy auth only after compatibility window, rollback evidence, and operator approval. |
| Auth migration execution | Migration | Phase 23.5 adds no execution endpoint or data mutation. | Migration plans may remain theoretical without controlled execution tooling. | Before Auth Service cutover | Build append-only migration execution, dry-run, approval, rollback, and evidence tooling. |
| External RNG provider sourcing | Integration | Provider selection is not approved yet. | Launch game may lack certified result source. | Before draw/RNG launch | Select provider and define contract tests. |
| PRNG certification strategy | Compliance | Jurisdiction requirements are not finalized. | Internal RNG may be unsuitable for production use. | Before internal RNG production use | Define certification evidence and approval workflow. |
| Strict regulatory RNG certification if jurisdiction requires | Compliance | Depends on market and regulator. | Launch may fail compliance review. | Before regulated launch | Confirm jurisdiction requirements. |
| Results comparison engine | Game Integrity | Production result providers are not selected. | Manual/provider result discrepancies may lack review workflow. | Before multi-provider results | Design comparison and dispute handling. |
| Production Game Engine deployment hardening | Infrastructure | Phase 22.6A creates only a skeleton. | Service may lack production security and observability. | Before Game Engine production traffic | Add auth, metrics, alerting, scaling, and runbooks. |
| Official result correction workflow | Game Integrity | Phase 22.6D enforces one official result and defers correction/replacement. | Incorrect certified result may lack governed remediation. | Before production draw certification | Define append-only correction and supersession workflow. |
| External feed provider implementation | Integration | Official feed provider is a placeholder. | State/official draw products cannot ingest certified results automatically. | Before official-feed games launch | Implement provider contract, auth, polling/webhook validation, and replay safety. |
| Production PRNG implementation | Game Integrity | Internal Production PRNG is a placeholder and not production-ready. | Internal proprietary games may lack approved result generation. | Before internal RNG games launch | Implement non-reproducible CSPRNG provider with certification evidence. |
| PRNG approval evidence | Compliance | Approval metadata is placeholder-only. | Internal RNG may be used without documented approval. | Before internal RNG production use | Store approval references, certification artifacts, and operator acceptance. |
| Draw result comparison engine | Game Integrity | Multi-source comparison is deferred. | Conflicting draw results may not be detected before certification. | Before multi-provider result intake | Build comparison, discrepancy, and operator review workflows. |
| Production statistical implementation | Certification | Phase 22.6E registers statistical framework placeholders only. | Certification package may lack regulator-ready statistical evidence. | Before certification submission | Implement validated statistical algorithms with repeatable fixtures. |
| Chi-square implementation | Certification | Statistical algorithm implementation is deferred. | Distribution anomalies may not be detected by certification harness. | Before internal RNG approval | Implement and validate chi-square checks. |
| Runs implementation | Certification | Statistical algorithm implementation is deferred. | Sequence-quality evidence may be incomplete. | Before internal RNG approval | Implement runs tests with threshold configuration. |
| Serial correlation | Certification | Statistical algorithm implementation is deferred. | Correlation defects may not be detected. | Before internal RNG approval | Implement serial-correlation analysis. |
| Continuous production monitoring | Operations | Production draw monitoring is not required until production draw generation exists. | RNG or provider drift may go unnoticed after launch. | Before production draw traffic | Add continuous draw-quality monitoring and alerting. |
| Certification PDF export | Compliance | Phase 22.6E emits structured packages only. | External reviewers may require human-readable packages. | Before external laboratory submission | Generate signed PDF/export artifacts from structured packages. |
| External laboratory submission automation | Compliance | External lab process is not selected. | Certification transfer may remain manual and error-prone. | Before external lab certification | Automate package transfer once lab requirements are known. |
| External RNG provider implementation | Integration | No external RNG provider has been selected. | External RNG games cannot launch. | Before external RNG games launch | Implement provider adapters and certification evidence capture. |
| Hardware RNG support | Infrastructure | Hardware RNG requirements are not known yet. | Jurisdiction or partner may require unsupported entropy source. | Before hardware RNG mandate | Add hardware RNG provider contract and health evidence. |
| Persistent scheduler storage | Game Engine | Phase 22.6F scheduler state is in-memory only. | Draw lifecycle diagnostics may not survive restart. | Before production scheduler activation | Persist schedules, lifecycle records, and recovery markers append-only. |
| Production scheduler activation | Game Engine | Scheduler framework is diagnostic-only. | Games cannot rely on automated draw lifecycle transitions. | Before production game launch | Add operator approval gate and production runtime controls. |
| Official feed polling | Integration | Official feed providers are placeholders. | Official/manual games may require manual result entry only. | Before official-feed games launch | Implement provider polling/webhook intake and replay protection. |
| Draw cancellation workflow | Game Integrity | Cancellation state exists, but governed workflow is deferred. | Cancelled draws may lack approval, audit, and player communication workflow. | Before production game launch | Implement append-only cancellation approvals and diagnostics. |
| Draw correction workflow | Game Integrity | Official result correction remains deferred. | Incorrect results may lack governed correction path. | Before production draw certification | Implement supersession/correction workflow with audit evidence. |
| Durable evaluation storage | Game Engine | Phase 22.6K adds replay-safe repository semantics but keeps the skeleton storage in process memory. | Evaluation evidence may not survive restart and cannot support production replay. | Before production evaluation activation | Back the repository with durable database tables and append-only constraints. |
| Production RabbitMQ batch processing | Game Engine | Phase 22.6G defines work item contracts but does not wire production queues. | Evaluation cannot scale across workers or recover from process loss. | Before production evaluation activation | Add approved queue topology, consumers, idempotent dispatch, and worker observability. |
| Ticket read integration | Game Engine | Phase 22.6G uses placeholder eligible ticket counts and does not read ticket data. | Production evaluation may miss or duplicate tickets without a governed read model. | Before production evaluation activation | Implement snapshot-safe ticket read abstraction and contract tests. |
| Settlement consumption integration | Settlement | Evaluation records are not consumed by Settlement in Phase 22.6G. | Winning tickets cannot be financially settled from Game Engine records. | Before production payout flow | Add Settlement consumer integration with idempotency and reconciliation evidence. |
| Evaluation replay/correction workflow | Game Integrity | Replay, correction, and supersession rules require operator governance. | Incorrect evaluations may lack an auditable remediation path. | Before production game launch | Design append-only replay/correction workflow with approvals and impact reporting. |
| Production RabbitMQ consumer activation | Game Engine | Phase 22.6H defines queue contracts and consumer skeleton only. | Evaluation batches will not be processed by real distributed workers. | Before production evaluation activation | Add approved hosted consumers, broker credentials, scaling policy, and replay safety. |
| Persistent queue state | Game Engine | Phase 22.6H queue diagnostics are in-memory. | Queue evidence may not survive restart and cannot support production reconciliation. | Before production evaluation activation | Persist queue publication, acknowledgement, retry, and DLQ evidence append-only. |
| Game Module execution integration | Game Engine | Phase 22.6H consumer simulates placeholder processing and does not execute modules. | Production tickets cannot be evaluated against game rules. | Before production game launch | Invoke approved Game Modules through contract-validated adapters with deterministic fixtures. |
| Settlement event publishing | Game Engine | Phase 22.6H does not emit settlement-consumable events. | Winning evaluations cannot flow to payout processing. | Before production payout flow | Publish immutable evaluation-completed events for Settlement with idempotent consumption. |
| DLQ operator workflow | Operations | Phase 22.6H has diagnostic dead-letter review only. | Poison or exhausted messages may lack governed triage and remediation. | Before production evaluation activation | Add DLQ review approvals, replay rules, audit trail, and alerting. |
| Production Keno paytable configuration | Game Integrity | Phase 22.6I uses fixture-safe paytable mappings only. | Product-specific payouts may be incomplete or incorrect if not formally configured. | Before Keno-family product launch | Add approved, versioned, product-specific paytables with contract fixtures. |
| Keno official feed integration | Integration | Phase 22.6I validates manual/official-shaped draw payloads but does not ingest feeds. | Official Keno-family products may require manual result entry only. | Before official Keno-family product launch | Implement official feed adapters, replay protection, and certification evidence. |
| Internal Keno draw generation approval | Compliance | Phase 22.6I keeps internal draw generation disabled by default. | Internal Keno-style games may lack approved draw generation. | Before internal Keno RNG games launch | Complete RNG certification and operator approval before enabling generation. |
| Keno performance benchmarking | Performance | Phase 22.6I focuses on correctness fixtures, not throughput limits. | Production evaluation capacity may be unknown. | Before production Keno traffic | Benchmark batch evaluation, derived wager paths, and paytable lookup. |
| Settlement consumption for Keno evaluations | Settlement | Phase 22.6I produces evaluation output only and does not integrate payout flow. | Winning Keno evaluations cannot settle financially. | Before production payout flow | Add Settlement consumption with idempotency, reconciliation, and rollback evidence. |
| Database ticket reader production binding | Game Engine | Phase 22.6K adds the database-shaped reader contract with deterministic skeleton data only. | Production execution cannot evaluate real purchased tickets. | Before production evaluation activation | Bind the reader to snapshot-safe ticket tables with range/cursor fixtures. |
| Settlement consumer for evaluation records | Settlement | Phase 22.6K persists evaluation records but does not publish or consume settlement events. | Winning tickets cannot create payout workflows. | Before production payout flow | Add idempotent Settlement consumer and reconciliation evidence. |
| Long-term evaluation archive strategy | Operations | Phase 22.6K exposes current evaluation records only. | High-volume historical evaluation evidence may grow without retention or archival rules. | Before production game launch | Define retention, cold storage, search, and export rules for immutable evaluation evidence. |
| Settlement consumer activation | Settlement | Phase 22.6L models the activation gate but keeps it disabled. | Winning evaluations may remain settlement-ready but unconsumed. | Before production payout flow | Add operator approval, activation controls, and integration QA evidence. |
| Settlement event publishing | Game Engine | Phase 22.6L does not emit settlement-consumable events. | Settlement cannot subscribe to completed evaluation records. | Before production payout flow | Publish immutable evaluation-ready events through the approved outbox path. |
| Durable repository DB wiring | Game Engine | Phase 22.6L creates schema drafts and contracts but keeps runtime storage in process memory. | Records and checkpoints may not survive process restart. | Before production evaluation activation | Implement database-backed repositories with transaction and retry evidence. |
| Append-only DB trigger enforcement | Game Engine | Phase 22.6L drafts append-only triggers but does not apply migrations automatically. | Database-level immutability may remain unproven in active environments. | Before production evaluation activation | Apply and verify trigger-backed update/delete prevention through migration QA. |
| Consumed-status governance | Settlement | Phase 22.6L includes future consumed-status fields but no governed update workflow. | Settlement consumption markers may lack approval, idempotency, or audit policy. | Before production payout flow | Define append-only consumption acknowledgements and reconciliation views. |
| Re-settlement and re-evaluation policy | Game Integrity | Phase 22.6L does not define correction, supersession, or financial impact workflows. | Corrected draws or evaluation defects may lack governed remediation. | Before production game launch | Design re-evaluation, supersession, re-settlement, and player-impact approvals. |
| Replay execution | Game Integrity | Phase 22.6J has no replay execution model. | Corrected draws or module fixes may lack governed re-evaluation. | Before production game launch | Design replay, supersession, and immutable replacement evidence. |
| Operator replay tooling | Operations | Phase 22.6J exposes diagnostics only. | Operators may lack a safe way to inspect and approve replay actions. | Before production game launch | Add approval-gated replay tooling, impact previews, and audit events. |
